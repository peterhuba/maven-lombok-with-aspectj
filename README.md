# maven-lombok-with-aspectj

### Using Lombok with aspectj together with unit tests

* `mvn clean install -DskipTests`
* `mvn exec:run`
* `mvn test`

If using Intellij IDEA to test/run make sure to use `javac` compiler (not `ajc`).
